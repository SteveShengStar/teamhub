import styled from 'styled-components';
import { SystemLabel } from './SystemComponents';

const Label = styled(SystemLabel)``;
Label.defaultProps = {
  fontFamily: 'body',
  fontSize: 'body2'
};

export default Label;